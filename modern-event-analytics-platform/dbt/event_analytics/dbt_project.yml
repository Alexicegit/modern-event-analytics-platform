name: event_analytics
version: '1.0.0'
config-version: 2

profile: event_analytics

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  event_analytics:
    staging:
      +schema: STAGING
      +materialized: view
    marts:
      +schema: MART
      +materialized: table
